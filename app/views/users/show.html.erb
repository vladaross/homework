<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>


</h1>
  <p>Accounts: </p>
</h1>

<table>
  <thead>
    <tr>
      <th>Account</th>
      <th>Amount</th>
      <th>Currency</th>
      <th>Actions</th>


      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user.accounts.each do |account| %>
      <tr>
        <td><%= account.id%></td>
        <td><%= account.amount %></td>
        <td><%= account.currency%></td>
        <td><%= link_to 'New Transaction', new_account_transaction_path(@account) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
